{
	"compilerOptions": {
		"outDir": ".tmp/",
		"rootDir": "./",

		"strict": true,
		"jsx": "preserve",
		"declaration": true,
		"declarationMap": true,
		"sourceMap": true,

		"experimentalDecorators": true,
		"target": "ESNext",
		"module": "ESNext",
		"moduleResolution": "node",
		"lib": ["dom", "ESNext"],

		"types": [],
		"typeRoots": [],

		// TODO, disable this, and see the type error with Behavior and
		// PropReceiver regarding declaration output (Behavior's getter override a
		// readonly property, which is wrong). Report the bug to TypeScript if
		// it still exists.
		"skipLibCheck": true
	},
	"include": ["./src/**/*"]
}
