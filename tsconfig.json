{
    "extends": "./node_modules/builder-js-package/config/tsconfig.json",
    "compilerOptions": {
        // TODO update this comment to document exactly why we need this option.
        "allowSyntheticDefaultImports": true,
        "declaration": false // TODO, set back to true after removing class-factory mixins
    }
}
