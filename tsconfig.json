{
	"extends": "./node_modules/@lume/cli/config/ts.config.json",

	// Exclude these as they are type-only files for users to optionally import,
	// they do not need to be compiled.
	"exclude": ["./src/**/*.react-jsx.d.ts"]
}
